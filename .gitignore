node_modules/
.next/
out/

# Local environment files and secrets. Keep only the sanitized template in Git.
.env
.env.local
.env.*
!.env.example
.vercel/

data/*.ndjson
!.gitkeep
.tmp/
npm-debug.log*
*.tsbuildinfo
backups/
.dev-server.log

# Local-only working docs and agent skills (not part of deployable app).
.opencode/
design-system/
